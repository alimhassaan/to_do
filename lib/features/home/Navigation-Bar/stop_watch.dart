import 'package:flutter/material.dart';

class StopWatchTimer extends StatelessWidget {
  const StopWatchTimer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Stop watch page"),
      ),
    );
  }
}