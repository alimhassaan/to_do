import 'package:flutter/material.dart';

class TimerWatch extends StatelessWidget {
  const TimerWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Timer Bage"),
      ),
    );
  }
}